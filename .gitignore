# regenerable intermediates (rebuild via pipeline/build_dtm.py)
*.npy

# raw LiDAR is ~281 MB and lives outside the repo
*.laz
*.las

# large regenerable pipeline intermediates (web copies live in public/data)
pipeline/data/city/
pipeline/data/processed/*.geojson

# python
__pycache__/
*.pyc
.venv/

# next.js / node
node_modules/
.next/
out/
.vercel/
*.tsbuildinfo
npm-debug.log*

.vercel
